<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.encycode.sheetalfoods">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SheetalFoods">
        <activity android:name=".EditOrderDetails"></activity>
        <activity android:name=".OrderDetails" />
        <activity android:name=".Notifications" />
        <activity android:name=".ViewOrders" />
        <activity android:name=".DealerCreateOrder" />
        <activity android:name=".StaffCreateOrder" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/No.Theme.SheetalFoods" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>